@model IEnumerable<FavoriteModel>
@*Generated from Custom Template.*@
@*Model namespace using directive should be added to _ViewImports.cshtml.*@

@{
    ViewData["Title"] = "Chef's Recommendations";
}

<h1>@ViewData["Title"]</h1>

<p>
    <a asp-action="Clear">Clear Recommendations</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UserName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.GameName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SalesPriceOutput)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.UserName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.GameName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.SalesPriceOutput)
                </td>
                <td>
                    <a asp-action="Remove" asp-route-gameId="@item.GameId">Remove from Recommendations</a>
                </td>
            </tr>
        }
    </tbody>
</table>



